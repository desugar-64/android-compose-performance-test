description=This test measures performance of animating layout positions using the `offset` modifier in Jetpack Compose. ⚠️ Visually, it is identical to the canvas test.
procedure=Adds 500 Box layouts and moves them around, updating their positions using `Modifier.offset { ... }`.